# DevPanel site presets
#
# Syntax: internal-value = Visible label
# Add or remove entries to customize Advanced setup. Built-in values receive
# a native starter where available; other values create a safe editable folder
# with NOTES.txt. Categories are chosen in the DevPanel UI, not by this file.

php = PHP Application
wordpress = WordPress
laravel = Laravel
blesta = Blesta
whmcs = WHMCS
astro = Astro (Node.js)
python = Python Application

# Popular frameworks and starters
symfony = Symfony
express = Express.js
nextjs = Next.js
react = React
django = Django
flask = Flask

# Popular CMS and web applications
drupal = Drupal
prestashop = PrestaShop
magento = Magento Open Source
joomla = Joomla

# Kept for existing installations that already use this preset value.
empty = Empty PHP folder

# Examples of user-defined presets:
# drupal = Drupal
# symfony = Symfony
# legacy-php = Legacy PHP application
